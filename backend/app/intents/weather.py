def get_weather(city: str) -> str:
    """
    por ahora regresa un clima inventado
    Fase 1.5: aquí se conecta a una api real (OpenWeatherMap).
    """
    return f"En {city} están 23 °C y soleado."